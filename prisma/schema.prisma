generator client {
  provider = "prisma-client-js"
  engineType = "binary"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// --- ACTIVE MODELS ---

model AdminUser {
  id         Int       @id @default(autoincrement())
  email      String    @unique(map: "AdminUser_email_key")
  password   String
  createdAt  DateTime  @default(now())
  updatedAt  DateTime @default(now()) @updatedAt
  name       String    @default("Admin")
  otp        String?   @db.VarChar(10)
  otp_expiry DateTime? @db.DateTime(0)

  @@map("adminuser")
}

model BlogPost {
  id              Int      @id @default(autoincrement())
  title           String
  slug            String   @unique(map: "BlogPost_slug_key")
  content         String   @db.Text
  imageUrl        String?
  imageAlt        String?
  metaTitle       String?
  metaDescription String?  @db.Text
  published       Boolean  @default(false)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @default(now()) @updatedAt
  altText         String?  @db.VarChar(255)

  @@map("blogpost")
}

model ContentBlock {
  id        Int      @id @default(autoincrement())
  key       String   @unique(map: "ContentBlock_key_key")
  value     String   @db.Text
  page      String
  createdAt DateTime @default(now())
  updatedAt DateTime @default(now()) @updatedAt

  @@map("contentblock")
}

model Faq {
  id        Int      @id @default(autoincrement())
  question  String   @db.Text
  answer    String   @db.Text
  order     Int?
  createdAt DateTime @default(now())
  updatedAt DateTime @default(now()) @updatedAt

  @@map("faq")
}

model HomeFeature {
  id           Int      @id @default(autoincrement())
  sectionId    Int
  icon         String?  @default("✨") @db.VarChar(50)
  title        String   @db.VarChar(255)
  description  String   @db.Text
  displayOrder Int?     @default(0)
  isActive     Boolean? @default(true)
  createdAt    DateTime @default(now()) @db.Timestamp(0)
  updatedAt  DateTime @default(now()) @updatedAt @db.Timestamp(0)

  @@map("homefeature")
}

model HomeSection {
  id           Int      @id @default(autoincrement())
  title        String   @db.VarChar(255)
  subtitle     String?  @db.VarChar(255)
  content      String?  @db.Text
  imageUrl     String?  @db.VarChar(255)
  sectionType  String?  @default("custom") @db.VarChar(50)
  displayOrder Int?     @default(0)
  isActive     Boolean? @default(true)
  createdAt    DateTime @default(now()) @db.Timestamp(0)
  updatedAt    DateTime @default(now()) @updatedAt @db.Timestamp(0)

  @@map("homesection")
}

model Order {
  id            Int         @id @default(autoincrement())
  customerName  String
  customerPhone String
  customerEmail String?
  address       String      @db.Text
  city          String
  totalAmount   Float
  paymentMethod String
  status        String      @default("Processing")
  items         String?     @db.LongText
  createdAt     DateTime    @default(now())
  updatedAt     DateTime @default(now()) @updatedAt
  paymentStatus String      @default("PENDING")
  transactionId String?
  userId        Int?
  user          User?       @relation(fields: [userId], references: [id], map: "Order_userId_fkey")
  orderitem     OrderItem[]

  @@index([userId], map: "Order_userId_fkey")
  @@map("order")
}

model OrderItem {
  id        Int     @id @default(autoincrement())
  orderId   Int
  productId Int
  quantity  Int
  price     Float
  order     Order   @relation(fields: [orderId], references: [id], map: "OrderItem_orderId_fkey")
  product   Product @relation(fields: [productId], references: [id], map: "OrderItem_productId_fkey")

  @@index([orderId], map: "OrderItem_orderId_fkey")
  @@index([productId], map: "OrderItem_productId_fkey")
  @@map("orderitem")
}

model PaymentGateway {
  id         Int      @id @default(autoincrement())
  name       String   @unique(map: "PaymentGateway_name_key")
  enabled    Boolean  @default(false)
  mode       String   @default("test")
  testApiKey String?
  testSecret String?
  liveApiKey String?
  liveSecret String?
  updatedAt  DateTime @default(now()) @updatedAt

  @@map("paymentgateway")
}

model Product {
  id             Int         @id @default(autoincrement())
  name           String
  description    String      @db.Text
  price          Float
  volume         String
  imageUrl       String?
  stock          Int         @default(0)
  createdAt      DateTime    @default(now())
  updatedAt     DateTime @default(now()) @updatedAt
  healthBenefits String?     @db.Text
  howToUse       String?     @db.Text
  images         String?     @db.LongText
  orderitem      OrderItem[]

  @@map("product")
}

// Settings model removed and consolidated into SiteSettings

model SiteSettings {
  id             Int     @id @default(autoincrement())
  siteName       String? @default("Auxiron Ghee") @db.VarChar(255)
  siteTagline    String? @default("Identity of Purity") @db.VarChar(255)
  logoUrl        String? @db.VarChar(255)
  heroHeading    String? @default("Shuddh Desi Bilona Ghee") @db.VarChar(255)
  heroSubheading String? @default("Pure by Tradition. Trusted for Generations.") @db.Text
  heroBgUrl      String? @db.VarChar(255)
  heroBgMobileUrl String? @db.VarChar(255)
  contactEmail   String? @db.VarChar(255)
  contactPhone   String? @db.VarChar(20)
  whatsappNumber String? @db.VarChar(20)
  officeAddress  String? @db.Text
  returnPolicy   String? @db.Text
  shippingPolicy String? @db.Text
  privacyPolicy  String? @db.Text
  updatedAt      DateTime @default(now()) @updatedAt

  @@map("sitesettings")
}

model SMTPSettings {
  id             Int      @id @default(autoincrement())
  smtpHost       String   @db.VarChar(255)
  smtpPort       String   @db.VarChar(10)
  smtpUsername   String   @db.VarChar(255)
  smtpPassword   String   @db.VarChar(255)
  smtpEncryption String   @db.VarChar(10)
  senderEmail    String   @db.VarChar(255)
  senderName     String   @db.VarChar(255)
  updatedAt      DateTime @default(now()) @updatedAt @db.Timestamp(0)
  otpSubject     String?  @default("Your OTP Code - Auxiron") @db.VarChar(255)
  otpTemplate    String?  @db.Text

  @@map("smtpsettings")
}

model Testimonial {
  id           Int      @id @default(autoincrement())
  sectionId    Int?
  name         String   @db.VarChar(255)
  location     String?  @db.VarChar(255)
  review       String   @db.Text
  rating       Int?     @default(5)
  displayOrder Int?     @default(0)
  isActive     Boolean? @default(true)
  createdAt    DateTime @default(now()) @db.Timestamp(0)
  updatedAt    DateTime @default(now()) @updatedAt @db.Timestamp(0)

  @@map("testimonial")
}

model User {
  id         Int       @id @default(autoincrement())
  name       String
  email      String    @unique(map: "User_email_key")
  password   String
  createdAt  DateTime  @default(now())
  updatedAt  DateTime @default(now()) @updatedAt
  otp        String?   @db.VarChar(10)
  otp_expiry DateTime? @db.DateTime(0)
  isVerified Boolean   @default(false)
  location   String?   @db.VarChar(255)
  order      Order[]

  @@map("user")
}

// --- LEGACY MODELS (UNUSED - COMMENTED OUT TO CLEAN UP PRISMA CLIENT) ---
/*
model AboutSection {
  id        Int      @id @default(autoincrement())
  heading   String   @db.VarChar(255)
  content   String   @db.Text
  imageUrl  String   @db.VarChar(255)
  createdAt DateTime @default(now()) @db.Timestamp(0)
  updatedAt DateTime @default(now()) @updatedAt @db.Timestamp(0)
  @@map("aboutsection")
}

model AdminUsersLegacy {
  id        Int       @id @default(autoincrement())
  username  String    @unique(map: "username") @db.VarChar(100)
  password  String    @db.VarChar(255)
  role      String?   @default("admin") @db.VarChar(50)
  createdAt DateTime? @default(now()) @db.DateTime(0)
  @@map("admin_users")
}

model BlogPostsLegacy {
  id              Int       @id @default(autoincrement())
  title           String    @db.VarChar(255)
  slug            String    @unique(map: "slug") @db.VarChar(255)
  content         String    @db.Text
  metaTitle       String?   @db.VarChar(255)
  metaDescription String?   @db.Text
  altText         String?   @db.VarChar(255)
  published       Boolean?  @default(true)
  createdAt       DateTime? @default(now()) @db.DateTime(0)
  updatedAt       DateTime? @default(now()) @db.DateTime(0)
  @@map("blog_posts")
}

model FaqsLegacy {
  id       Int    @id @default(autoincrement())
  question String @db.VarChar(255)
  answer   String @db.Text
  order    Int?   @default(0)
  @@map("faqs")
}

model OrdersLegacy {
  id            Int       @id @default(autoincrement())
  customerName  String    @db.VarChar(255)
  customerPhone String    @db.VarChar(20)
  customerEmail String?   @db.VarChar(255)
  address       String    @db.Text
  city          String    @db.VarChar(100)
  totalAmount   Decimal   @db.Decimal(10, 2)
  paymentMethod String    @db.VarChar(50)
  status        String?   @default("Processing") @db.VarChar(50)
  items         String    @db.LongText
  userId        Int?
  createdAt     DateTime? @default(now()) @db.DateTime(0)
  updatedAt     DateTime? @default(now()) @db.DateTime(0)
  @@map("orders")
}

model PaymentGatewaysLegacy {
  id          Int                    @id @default(autoincrement())
  gatewayName String                 @db.VarChar(50)
  isEnabled   Boolean?               @default(false)
  apiKey      String?                @db.VarChar(255)
  apiSecret   String?                @db.VarChar(255)
  mode        String?
  @@map("payment_gateways")
}

model ProductsLegacy {
  id              Int       @id @default(autoincrement())
  name            String    @db.VarChar(255)
  description     String    @db.Text
  price           Decimal   @db.Decimal(10, 2)
  volume          String    @db.VarChar(50)
  stock           Int?      @default(0)
  images          String?   @db.LongText
  health_benefits String?   @db.Text
  how_to_use      String?   @db.Text
  createdAt       DateTime? @default(now()) @db.DateTime(0)
  updatedAt       DateTime? @default(now()) @db.DateTime(0)
  @@map("products")
}

model SiteSettingsLegacy {
  id             Int     @id @default(autoincrement())
  siteName       String? @default("Auxiron Ghee") @db.VarChar(255)
  siteTagline    String? @default("Identity of Purity") @db.VarChar(255)
  logoUrl        String? @db.VarChar(255)
  heroHeading    String? @default("Shuddh Desi Bilona Ghee") @db.VarChar(255)
  heroSubheading String? @default("Pure by Tradition. Trusted for Generations.") @db.Text
  heroBgUrl      String? @db.VarChar(255)
  contactEmail   String? @db.VarChar(255)
  contactPhone   String? @db.VarChar(20)
  whatsappNumber String? @db.VarChar(20)
  officeAddress  String? @db.Text
  returnPolicy   String? @db.Text
  shippingPolicy String? @db.Text
  privacyPolicy  String? @db.Text
  @@map("site_settings")
}

model TestimonialsLegacy {
  id           Int       @id @default(autoincrement())
  customerName String    @db.VarChar(255)
  rating       Int?      @default(5)
  content      String    @db.Text
  createdAt    DateTime? @default(now()) @db.DateTime(0)
  @@map("testimonials")
}

model UsersLegacy {
  id        Int       @id @default(autoincrement())
  name      String    @db.VarChar(255)
  email     String    @unique(map: "email") @db.VarChar(255)
  password  String    @db.VarChar(255)
  phone     String?   @db.VarChar(20)
  address   String?   @db.Text
  createdAt DateTime? @default(now()) @db.DateTime(0)
  updatedAt DateTime? @default(now()) @db.DateTime(0)
  @@map("users")
}
*/

enum paymentgatewayconfig_mode {
  test
  live
}

enum payment_gateways_mode {
  test
  live
}
